# REV Framework Requirements
# Core dependencies for the Restriction Enzyme Verification system

# Core ML/AI
torch>=2.0.0,<3.0.0
transformers>=4.30.0,<5.0.0
numpy>=1.21.0,<2.0.0
scipy>=1.7.0,<2.0.0
scikit-learn>=1.0.0,<2.0.0

# Hyperdimensional Computing
pandas>=1.3.0,<3.0.0

# Cryptography and Security
cryptography>=41.0.0,<42.0.0
pycryptodome>=3.15.0,<4.0.0
PyYAML>=6.0,<7.0.0

# API and Web
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.20.0,<1.0.0
redis>=4.5.0,<6.0.0
python-jose[cryptography]>=3.3.0,<4.0.0
python-multipart>=0.0.6,<1.0.0
httpx>=0.24.0,<1.0.0

# System and Performance
psutil>=5.9.0,<6.0.0
tqdm>=4.65.0,<5.0.0

# Optional: Quantization support
# Uncomment if you want to use quantization features
# bitsandbytes>=0.41.0,<1.0.0  # For 8-bit and 4-bit quantization

# Optional: GPU acceleration
# Uncomment for CUDA support
# cupy-cuda11x>=11.0.0,<13.0.0  # Replace with appropriate CUDA version

# Optional: Additional cryptography
# pyblake2>=1.1.2,<2.0.0  # For BLAKE2b hashing (faster than SHA256)

# Development dependencies (install with pip install -r requirements-dev.txt)
# See requirements-dev.txt for testing and development tools